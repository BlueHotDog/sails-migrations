/*
 * Auto generated config by Gake
 * Please visit http://gruntjs.com/ to learn more about how to work with grunt config options
 * Config options are mapped by folder, so a file under the following path:
 * /config/some/name.js
 * will create the following key: grunt.config('some.name') and each exported key will be under that namespace
 * */
module.exports = {
  /*
    add config keys here,
    you'll be able to access to those keys under the '{{=it.name}}' namespace
   */
};
